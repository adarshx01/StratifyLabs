'use client'

const TopBar = () => {
  return (
    <div className="w-full bg-blue-600 text-white text-center py-1 shadow-md">
      <p className="text-base font-medium">
        Everything you need to train cutting-edge computer vision models — all in one place.
      </p>
    </div>
  )
}

export default TopBar